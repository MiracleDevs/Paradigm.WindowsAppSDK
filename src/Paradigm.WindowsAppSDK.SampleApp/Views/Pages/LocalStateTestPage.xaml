<Page
    x:Class="Paradigm.WindowsAppSDK.SampleApp.Views.Pages.LocalStateTestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Paradigm.WindowsAppSDK.SampleApp.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center">
        <TextBlock Text="This is a test page with local state storage access" TextAlignment="Center" />
        <Button Content="Go back" Click="{x:Bind ViewModel.ExecuteActionAsync}" />
        <TextBlock Text="{x:Bind ViewModel.StorageFilesHeaderText, Mode=OneWay}" ></TextBlock>
        <ListBox ItemsSource="{x:Bind ViewModel.StorageFiles, Mode=OneWay}"></ListBox>
    </StackPanel>
</Page>
